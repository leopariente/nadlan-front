const base =
  'rounded border border-slate-200 bg-white px-2 py-1.5 text-sm text-right tabular-nums text-slate-800 ' +
  'focus:outline-none focus:ring-2 focus:ring-blue-500'

/** Wide input (w-28) — used in Table 1, 2, 3 */
export const inputCls   = `w-28 ${base}`

/** Narrow input (w-20) — used in Table 4, 5 */
export const inputClsSm = `w-20 ${base}`
